#include <set>
#include <map>
#include <string>

void findRepeatedSubstrings(int, std::map<std::string, unsigned> &, std::set<unsigned> &);

unsigned findKeyLength(int);

void findDistanceDivisors(unsigned, std::map<unsigned, unsigned> &);
